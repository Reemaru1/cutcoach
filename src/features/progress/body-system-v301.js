'use strict';
(function(root){
  const ZONES={core:'Bauch & Core',chest:'Brust',shoulders:'Schultern',arms:'Arme',legs:'Beine'};
  let active='core',mounted=false;
  const q=(s,r=document)=>r.querySelector(s);
  const clamp=(v,min=0,max=1)=>Math.min(max,Math.max(min,Number(v)||0));
  const number=v=>v===null||v===undefined||v===''?null:(Number.isFinite(Number(v))?Number(v):null);
  function appState(){try{if(typeof state==='object'&&state)return state}catch{}return root.state&&typeof root.state==='object'?root.state:null}
  function weights(){return Object.entries(appState()?.days||{}).map(([date,d])=>({date,value:number(d?.weight)})).filter(x=>x.value>0).sort((a,b)=>a.date.localeCompare(b.date))}
  function profile(){return appState()?.profile||{}}
  function currentWeight(){const list=weights();return list.at(-1)?.value??number(profile().baselineWeight)}
  function startWeight(){const list=weights();return list[0]?.value??number(profile().baselineWeight)}
  function goalWeight(){return number(profile().goalWeight??appState()?.settings?.goalWeight)}
  function progress(){const start=startWeight(),current=currentWeight(),goal=goalWeight();if(!start||!current||!goal||Math.abs(goal-start)<.1)return 0;return clamp((current-start)/(goal-start))}
  function bodyWidth(){const height=number(profile().height),weight=currentWeight();if(!height||!weight)return 1;const bmi=weight/Math.pow(height/100,2);return clamp(.92+(bmi-20)*.012,.9,1.18)}
  function workoutScores(){const scores={core:0,chest:0,shoulders:0,arms:0,legs:0};const cutoff=new Date();cutoff.setHours(0,0,0,0);cutoff.setDate(cutoff.getDate()-6);for(const [date,data] of Object.entries(appState()?.days||{})){const day=new Date(date+'T12:00:00');if(Number.isNaN(day.getTime())||day<cutoff)continue;for(const ex of data?.workout?.exercises||[]){let key=ex?.muscle;if(key==='back')key='shoulders';if(key==='glutes')key='legs';if(scores[key]!==undefined)scores[key]+=Math.max(1,Number(ex?.sets)||1)}}return scores}
  function copy(zone){const p=progress(),score=workoutScores()[zone]||0;if(zone==='core')return p>0?`${Math.round(p*100)} % deiner dokumentierten Gewichtsreise sind erreicht. Die Form verändert sich nur anhand deiner echten Messwerte.`:'Gewicht und optional Taille regelmäßig erfassen, damit sich die Körperdarstellung belastbar entwickelt.';if(score>0)return `${score} protokollierte Arbeitssätze der letzten 7 Tage sind dieser Zone zugeordnet.`;return `Noch keine detaillierten Trainingsdaten für ${ZONES[zone]}.`}
  function markup(){return `<div class="cc301-body" role="group" aria-label="Interaktiver CutCoach Körper"><svg viewBox="0 0 260 520" role="img" aria-label="Dynamische Körperdarstellung"><defs><linearGradient id="cc301Skin" x1="0" y1="0" x2="1" y2="1"><stop stop-color="#e7fff7"/><stop offset=".48" stop-color="#75e5bd"/><stop offset="1" stop-color="#4e8ea9"/></linearGradient></defs><g class="cc301-model"><circle cx="130" cy="54" r="31"/><path d="M112 80h36l6 31h-48z"/><path d="M82 113Q130 92 178 113L194 230Q178 292 164 320H96Q82 292 66 230Z"/><path d="M77 123Q48 145 42 231L55 239Q70 174 91 153Z"/><path d="M183 123Q212 145 218 231L205 239Q190 174 169 153Z"/><path d="M101 311h25l-6 167q-7 19-26 8Z"/><path d="M134 311h25l7 175q-19 11-26-8Z"/></g><g class="cc301-zones"><g data-cc301-zone="core"><ellipse cx="130" cy="237" rx="43" ry="61"/></g><g data-cc301-zone="chest"><ellipse cx="108" cy="155" rx="28" ry="22"/><ellipse cx="152" cy="155" rx="28" ry="22"/></g><g data-cc301-zone="shoulders"><circle cx="82" cy="128" r="24"/><circle cx="178" cy="128" r="24"/></g><g data-cc301-zone="arms"><path d="M77 137 49 231M183 137 211 231"/></g><g data-cc301-zone="legs"><path d="M109 326 103 474M151 326 157 474"/></g></g></svg><div class="cc301-stage"><span>Körperreise</span><b id="cc301Percent">0%</b></div></div>`}
  function mount(){if(mounted)return true;const figure=q('.bp220-figure');if(!figure)return false;figure.classList.add('cc301-host');figure.insertAdjacentHTML('beforeend',markup());const insight=q('.bp220-insight');if(insight&&!q('.cc301-card',insight))insight.insertAdjacentHTML('afterbegin',`<article class="cc301-card"><small>INTERAKTIVE KÖRPERZONE</small><h3 id="cc301Title"></h3><p id="cc301Copy"></p><div class="cc301-tabs">${Object.entries(ZONES).map(([key,label])=>`<button type="button" data-cc301-tab="${key}">${label}</button>`).join('')}</div></article>`);mounted=true;return true}
  function render(){try{if(!mount())return false;const host=q('.cc301-host');if(!host)return false;const p=progress(),scores=workoutScores();host.style.setProperty('--cc301-progress',p);host.style.setProperty('--cc301-width',(bodyWidth()-p*.06).toFixed(3));host.dataset.activeZone=active;host.dataset.goal=profile().goal||'lose';for(const [zone,score] of Object.entries(scores))host.style.setProperty(`--cc301-${zone}`,clamp(score/16));const percent=q('#cc301Percent');if(percent)percent.textContent=`${Math.round(p*100)}%`;const title=q('#cc301Title');if(title)title.textContent=ZONES[active];const text=q('#cc301Copy');if(text)text.textContent=copy(active);document.querySelectorAll('[data-cc301-tab]').forEach(button=>button.classList.toggle('active',button.dataset.cc301Tab===active));return true}catch(error){console.warn('CutCoach Body System deaktiviert:',error);return false}}
  document.addEventListener('click',event=>{const target=event.target.closest?.('[data-cc301-zone],[data-cc301-tab]');if(!target)return;active=target.dataset.cc301Zone||target.dataset.cc301Tab||active;render()});
  root.CutCoachBodySystem301={render};
})(window);